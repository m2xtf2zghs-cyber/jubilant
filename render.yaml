services:
  - type: web
    name: money-lender-statement-analyser-api
    runtime: python
    rootDir: .
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn backend.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
